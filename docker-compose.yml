services:
  notifier:
    build:
      context: .
    image: turntf-notifier:local
    ports:
      - "8080:8080"
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./data:/app/data
    restart: unless-stopped
